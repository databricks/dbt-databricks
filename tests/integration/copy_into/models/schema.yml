version: 2

seeds:
  - name: source
    config:
      column_types:
        id: int
        name: string
        date: string
  - name: target
    config:
      column_types:
        id: int
        name: string
        date: string
