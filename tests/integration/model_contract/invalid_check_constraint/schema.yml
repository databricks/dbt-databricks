version: 2

models:
  - name: invalid_check_constraint
    config:
      contract:
        enforced: true
    constraints:
      - type: check
        name: invalid_constraint
        columns: []
        condition:

