default:
  outputs:
    dev:
      type: databricks
      method: dbsql
      schema: [schema_name]
      host: [yourorg.sparkhost.com]
      http_path: [HTTP Path]
      token: [abc123]

  target: dev
